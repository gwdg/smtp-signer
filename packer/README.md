# Packer builds for carp and haproxy vagrant setup

## Getting Started

  ./bootstrap.sh    # install packer
  ./clone.sh        # get templates
  ./build.sh        # build virtualbox box
  ./install.sh      # install vagrant box

